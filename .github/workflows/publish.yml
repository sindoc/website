name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - uses: actions/checkout@v4

      # Logseq Publish step
      - name: Logseq Publish 🚩
        uses: pengx17/logseq-publish@main
        with:
          version: "master"

      # Add a .nojekyll file to disable Jekyll on GitHub Pages
      - name: add a nojekyll file
        run: touch $GITHUB_WORKSPACE/www/.nojekyll

      # Deploy to GitHub Pages
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: www
          clean: true
          single-commit: true

      # Upload artifact (if applicable)
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: logseq-output
          path: path/to/output
